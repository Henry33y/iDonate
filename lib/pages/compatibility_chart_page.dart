import 'package:flutter/material.dart';

class CompatibilityChartPage extends StatelessWidget {
  const CompatibilityChartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: const Color(0xFFCC2B2B),
        title: const Text('Compatibility Chart'),
      ),
      body: const Center(
        child: Text('Compatibility Chart Page Coming Soon'),
      ),
    );
  }
} 